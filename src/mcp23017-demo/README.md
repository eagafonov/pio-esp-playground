# mcp23017-demo

Polls two MCP23017 I²C GPIO expanders and prints the combined 32-bit bus state with a clock counter over serial. Building block for the [logic-analyzer](../logic-analyzer/).

```bash
pio run -e mcp23017-demo -t upload
```
