# hello-world

Minimal "Hello, World!" over serial — the obligatory smoke test to prove the toolchain, the board, and the USB cable all work. Available in three board flavors (ESP8266, ESP32, ESP32-S3).

```bash
pio run -e hello-world -t upload
```
