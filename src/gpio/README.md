# gpio

GPIO playground: button interrupts with debounce, LED toggling, and a rotary encoder. Also pulses an LED as a make-believe CPU clock at various speeds while reading bus state via MCP23017 — the early ancestor of the [logic-analyzer](../logic-analyzer/).

```bash
pio run -e gpio -t upload
```
